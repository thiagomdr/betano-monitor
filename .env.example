# Painel le o Supabase via web/supabase.config.json (anon key publica).
# A coleta Betano roda na Edge Function (secrets automaticos SUPABASE_URL / SERVICE_ROLE).
#
# Opcional: proteger a function
# CRON_SECRET=

# Opcional: teste local screenshot + Gemini (scripts/screenshot-betano-odds.mjs)
# GEMINI_API_KEY=
