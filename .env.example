# Usado pelos scripts de build do painel (fill-historico-html.mjs) e GitHub Actions.
# Fallback local: web/historico/supabase.config.json
EXPO_PUBLIC_SUPABASE_URL=https://seu-projeto.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=sua-anon-key

# Secrets na Edge Function (Dashboard → Edge Functions → Secrets):
# CRON_SECRET=string-aleatoria-longa  (opcional; protege betano-coleta-cron)
